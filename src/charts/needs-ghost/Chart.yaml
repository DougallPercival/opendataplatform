# Copied and parameterized by `platform-cli module scaffold <name>` (platform-module-lifecycle
# branch, 2026-09-03) — see ARCHITECTURE.md §3 ("Building a new module") and
# docs/architecture/module-lifecycle-plan.md. needs-ghost/Needs Ghost are the two tokens
# scaffold substitutes; everything else is safe to leave as-is for a first module.
apiVersion: v2
name: needs-ghost
description: Needs Ghost — an OpenDataPlatform module.
version: 0.1.0
appVersion: "1.0"
