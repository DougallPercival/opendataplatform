# ui-shell

The one front door: unified nav built from module manifests, catalog browser, pipeline & run
status, deep-links into each module's own UI, workspace switcher. ARCHITECTURE.md §2. Not built
yet — comes together as `gateway` and `catalog-service` land in Phase 2.
