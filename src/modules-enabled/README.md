# modules-enabled

Which modules *this* deployment turned on — Argo CD watches this directory (ARCHITECTURE.md §3).
Empty until the first module is installed, whether that's by hand, `platform-cli module install`,
or the Add-ons page. Nothing to do here yet — Phase 0 is core-only.
