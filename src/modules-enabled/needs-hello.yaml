# GENERATED by `platform module install needs-hello` at 2026-09-04T02:35:22Z — do not hand-edit.
# Source descriptor: src/modules/needs-hello/module.yaml. To change this module's placement,
# namespace, or chart, edit that file and re-run `platform module install needs-hello`
# (it's safe to run again — it overwrites this file in place and commits the diff).
#
# Picked up by modules-root (src/core/argocd/apps/core/modules-root.yaml), which watches
# src/modules-enabled/ the same way root-app.yaml watches apps/core/ — see that file's header
# comment for the three-level app-of-apps structure. `syncPolicy.automated.prune: true` plus the
# finalizer below mean `platform module uninstall needs-hello` (which just deletes this file)
# is enough to tear the whole module back down — except any PersistentVolumeClaim the chart marks
# `argocd.argoproj.io/sync-options: Delete=false`, which survives on purpose (ARCHITECTURE.md §3;
# see src/charts/needs-hello/templates/*.yaml for this module's own PVCs, if it has any).
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: needs-hello
  namespace: argocd
  labels:
    platform.io/tier: module
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: git@github.com:DougallPercival/opendataplatform.git
    targetRevision: dev
    path: src/charts/needs-hello
    helm:
      values: |
        placement: {}
  destination:
    server: https://kubernetes.default.svc
    namespace: needs-hello
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
